function main() {
  $.getJSON('http://api.namegame.willowtreemobile.com/', function(result) { 
    makePersonsFromJSON(result);
  });
}

function makePersonsFromJSON(personsArray) {
  console.log(personsAray);
}

